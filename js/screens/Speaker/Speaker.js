import React from 'react';
import {
  View,
  Text,
  SafeAreaView,
  StyleSheet,
  Image,
  SectionList,
} from 'react-native';
import styles from './styles';

const Speaker = () => {
  return (
    <SafeAreaView>
      <Text style={styles.pageTitle}>Speaker</Text>
    </SafeAreaView>
  );
};

export default Speaker;
